# Vercel Python build installs from this file.
# Keep in sync with pyproject.toml [project.dependencies].
# fastmcp is held at 3.4.2 on purpose: the NEXT version, 3.4.3, enables Host validation
# (DNS-rebinding protection) that 421s every request behind Vercel's proxy. Bump deliberately
# after testing against prod domains.
fastmcp==3.4.2
psycopg[binary]>=3.2
pydantic>=2.7
httpx>=0.27
openpyxl>=3.1
posthog>=6
