# Mobile Workout Tracker Design

This is a code bundle for Mobile Workout Tracker Design. The original project is available at https://www.figma.com/design/lsVBEnL6s2IBYOBOcenHPP/Mobile-Workout-Tracker-Design.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
